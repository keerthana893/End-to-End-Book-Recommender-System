
# once yaml package is installed ..it will create egg-info folder ,means book recommender system is now a local package in our system
#, -e . - we can import it anywhere in our system and use it.---local package installation is done---egg file is created


streamlit==1.28.2
pandas==1.5.3
numpy==1.23.5
scikit-learn==1.2.2
pyyaml==6.0.1

